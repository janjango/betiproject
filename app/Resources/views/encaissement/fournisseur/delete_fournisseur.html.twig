<div id="modal-regular{{ element.id }}" class="modal" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form action="{{ path('delete_fournisseur',{'id': element.id } )}}" method="post" >
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
                <h3 class="modal-title text-left">Suppression</h3>
            </div>
            <div class="modal-body">
                Confirmez - vous la suppression de <strong>{{ element.nom }}</strong> ?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-sm btn-default" data-dismiss="modal">Fermer</button>
                <button type="submit" class="btn btn-sm btn-primary">Supprimer</button>
            </div>
        </form>
        </div>
    </div>
</div>